<Window x:Class="JsonConverter.LoadingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:JsonConverter"
        mc:Ignorable="d"
        Title="Veuillez patienter" 
        Height="150" 
        Width="300"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        ShowInTaskbar="False">
    <Grid>
        <Border BorderBrush="#FFABADB3" BorderThickness="1" Background="White" CornerRadius="5">
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <StackPanel Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <ProgressBar IsIndeterminate="True" Width="200" Height="20" Margin="0,0,0,10"/>
                    <TextBlock Text="Traitement en cours..." 
                               HorizontalAlignment="Center" 
                               FontSize="14" 
                               FontWeight="SemiBold"/>
                </StackPanel>
                
                <TextBlock Grid.Row="1" 
                           Text="Veuillez patienter pendant la conversion"
                           HorizontalAlignment="Center"
                           Margin="0,10,0,0"/>
            </Grid>
        </Border>
    </Grid>
</Window>
